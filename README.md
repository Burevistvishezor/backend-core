# backend-core
(CRUD)
# Bank SQLite – C++

Small C++ project demonstrating:
- SQLite3 integration
- Prepared statements
- Clean project structure
- CMake build system

## Build
```bash
sudo apt install sqlite3 libsqlite3-dev
mkdir build && cd build
cmake ..
make
./bank_sqlite
Create accounts

Store data in SQLite

List stored accounts
